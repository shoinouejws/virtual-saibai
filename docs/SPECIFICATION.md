# バーチャル栽培（Virtual Saibai） — 仕様・要件定義書

## 1. プロダクト概要

### 1.1 コンセプト

AIを活用し、様々な作物（野菜・果実）をバーチャルに栽培できるシミュレーションアプリ。
ユーザーは畑を耕し、種を植え、水や肥料を与えながら作物を育て、収穫を楽しむことができる。

**最大の特徴:** 収穫した作物は実際にユーザーのもとへ届けられる。バーチャルな栽培体験がリアルな報酬につながるユニークなサービスモデル。

### 1.2 プロダクトのゴール

- **フロントエンドプロトタイプ**の構築（栽培画面のみ）
- GitHub Pages で公開し、URLから誰でもアクセスできる形式にする

### 1.3 対象ユーザー

- カジュアルゲームを楽しむ一般ユーザー
- 農業・栽培に興味のあるユーザー

---

## 2. 技術要件

### 2.1 技術スタック

| 項目 | 技術 |
|------|------|
| フレームワーク | React + TypeScript |
| ビルドツール | Vite |
| スタイリング | Tailwind CSS |
| 状態管理 | React useState / useReducer（ローカルステート） |
| デプロイ | GitHub Pages（`main` ブランチからデプロイ） |
| パッケージ管理 | npm |

> **将来展望:** スマホアプリとしてのリリースを想定。React Native / Capacitor 等での展開を見据え、コンポーネント設計・状態管理ロジックの再利用性を意識する。

### 2.2 動作環境

- モダンブラウザ（Chrome, Firefox, Safari, Edge の最新版）
- レスポンシブ対応（モバイル・タブレット・デスクトップ）

### 2.3 制約事項

- フロントエンドのみ（バックエンド・データベースなし）
- データはブラウザの `localStorage` に保存
- プロトタイプ段階のため、実際の課金処理は実装しない（UIモックのみ）

---

## 3. 機能要件

### 3.1 栽培画面（メイン画面）

アプリのメイン画面。畑の状態と作物の成長を視覚的に表示し、ユーザーが栽培アクションを実行できる。

#### 3.1.1 画面構成

```
┌─────────────────────────────────────┐
│          ヘッダーエリア               │
│   アプリ名 / ステータス情報           │
├─────────────────────────────────────┤
│                                     │
│          畑・作物表示エリア            │
│   （畑のグリッド + 作物のビジュアル）   │
│                                     │
├─────────────────────────────────────┤
│        アクションボタンエリア          │
│  [耕す] [植える] [水やり] [肥料] [収穫]│
├─────────────────────────────────────┤
│     [🛒 ショップへ]（ページ遷移）       │
└─────────────────────────────────────┘
```

#### 3.1.2 UIコンポーネント一覧

| コンポーネント名 | 説明 |
|-----------------|------|
| `Header` | アプリ名、肥料在庫数、収穫数などのステータス表示 |
| `FarmGrid` | 畑のマス目をグリッド形式で表示（初期: 2×2 = 4マス） |
| `FarmCell` | 畑の個別マス。状態に応じてビジュアルが変化する |
| `CropDisplay` | マス内の作物の成長段階を表示（AI生成画像） |
| `ActionButtons` | 栽培アクションボタン群 |
| `ShopButton` | ショップ画面へ遷移するボタン |
| `ShopPage` | アイテム購入用の独立ページ |
| `NotificationArea` | アクション結果のフィードバックメッセージ表示 |

### 3.2 ユーザーアクション

ユーザーが実行できるアクションの一覧と、各アクションの仕様。

#### 3.2.1 土を耕す

| 項目 | 内容 |
|------|------|
| 対象 | 未使用状態のマス |
| 前提条件 | マスが「未使用」状態であること |
| 結果 | マスの状態が「耕された」に変化 |
| 表示変化 | 茶色い耕された土のビジュアルに変化 |

#### 3.2.2 種を植える

| 項目 | 内容 |
|------|------|
| 対象 | 耕された状態のマス |
| 前提条件 | マスが「耕された」状態であること |
| 操作 | 作物を選択して植える（作物選択UIを表示） |
| 結果 | マスの状態が「種まき済み」に変化 |
| 表示変化 | 種のアイコンが表示される |

**選択可能な作物（プロトタイプ）:**

プロトタイプでは1〜2種類に絞る。

| 作物 | 成長段階数 | 収穫までのアクション回数目安 |
|------|-----------|------------------------|
| トマト | 4 | 水やり3回 + 肥料1回 |
| いちご | 4 | 水やり3回 + 肥料1回 |

#### 3.2.3 水をやる

| 項目 | 内容 |
|------|------|
| 対象 | 種まき済み or 成長中のマス |
| 前提条件 | マスに作物が植えられていること |
| 結果 | 作物の成長ポイントが加算される |
| 表示変化 | 水滴アニメーション + 成長段階が進む場合はビジュアル変化 |

#### 3.2.4 肥料をまく

| 項目 | 内容 |
|------|------|
| 対象 | 種まき済み or 成長中のマス |
| 前提条件 | マスに作物が植えられていること、肥料の在庫があること |
| 結果 | 作物の成長ポイントが大幅に加算される（水やりの2倍） |
| 表示変化 | 肥料散布アニメーション + 成長段階が進む場合はビジュアル変化 |
| 消費 | 肥料アイテムを1つ消費 |

#### 3.2.5 収穫する

| 項目 | 内容 |
|------|------|
| 対象 | 作物が完全に実ったマス |
| 前提条件 | 作物が最終成長段階に到達していること |
| 結果 | 作物を収穫。マスは「耕された」状態に戻る。収穫した作物は「収穫リスト」に記録される |
| 表示変化 | 収穫アニメーション + 収穫おめでとう演出 |
| 報酬 | 収穫した作物が実際にユーザーへ届けられる（本番想定） |

> **プロトタイプでの扱い:** 実際の配送処理は実装しない。収穫時に「おめでとう！トマトが届きます」のような演出を表示し、収穫履歴として記録するのみ。将来的にはEC/物流システムとの連携を想定。

### 3.3 ショップ機能（課金画面）

栽培画面とは別の独立したページとして実装する。React Router 等でページ遷移する。
プロトタイプのため、実際の決済は発生しない（ボタン押下でアイテムが付与される）。

#### 3.3.1 購入可能アイテム

| アイテム | 価格 | 効果 |
|---------|------|------|
| 肥料 ×5 | ¥120（モック） | 肥料の在庫を5つ追加 |
| 肥料 ×15 | ¥300（モック） | 肥料の在庫を15つ追加 |
| 畑拡張（+2マス） | ¥500（モック） | 畑のマス数を2つ追加 |

#### 3.3.2 ショップ画面構成

独立した静的ページ。アイテム一覧を表示し、購入ボタンを配置する。

```
┌──────────────────────────────┐
│  [← 畑に戻る]     ショップ     │
├──────────────────────────────┤
│                              │
│  🧪 肥料 ×5     ¥120  [購入]  │
│  🧪 肥料 ×15    ¥300  [購入]  │
│  🌾 畑拡張      ¥500  [購入]  │
│                              │
└──────────────────────────────┘
```

> ショップは将来の課金機能のUIモック。購入ボタン押下時はプロトタイプ内で即座にアイテムが付与される。

---

## 4. データモデル

プロトタイプ段階では `localStorage` にJSON形式で保存する。

### 4.1 ゲーム状態（GameState）

```typescript
interface GameState {
  fertilizer: number;         // 肥料の在庫数
  farmSize: number;           // 畑のマス数
  cells: FarmCellState[];     // 各マスの状態
  harvestLog: HarvestRecord[]; // 収穫履歴
}

interface HarvestRecord {
  crop: CropType;
  harvestedAt: string;        // ISO 8601 日時
}
```

### 4.2 畑マスの状態（FarmCellState）

```typescript
interface FarmCellState {
  id: number;
  status: CellStatus;
  crop: CropType | null;
  growthPoints: number;     // 現在の成長ポイント
  maxGrowthPoints: number;  // 最大成長ポイント（収穫可能になる値）
  growthStage: number;      // 現在の成長段階（表示用）
  maxGrowthStage: number;   // 最大成長段階
}
```

### 4.3 列挙型

```typescript
type CellStatus = 'empty' | 'tilled' | 'planted' | 'growing' | 'harvestable';

type CropType = 'tomato' | 'strawberry';
```

### 4.4 作物定義（CropDefinition）

```typescript
interface CropDefinition {
  type: CropType;
  name: string;              // 表示名
  maxGrowthPoints: number;   // 収穫に必要な成長ポイント
  growthStages: number;      // 成長段階数
  stageImages: string[];     // 各成長段階のAI生成画像パス (例: ['/assets/tomato-1.png', ..., '/assets/tomato-4.png'])
}
```

---

## 5. 画面遷移・状態遷移

### 5.1 画面遷移

```
栽培画面（メイン） ⇄ ショップページ
```

2画面構成。React Router（HashRouter）を使ったページ遷移。

### 5.2 畑マスの状態遷移

```
[empty] ──(耕す)──▶ [tilled] ──(種を植える)──▶ [planted]
                       ▲                          │
                       │                    (水やり/肥料)
                       │                          ▼
                  (収穫する)◀── [harvestable] ◀── [growing]
                                              (成長ポイント
                                               が最大に到達)
```

### 5.3 成長ロジック

1. `水やり` → 成長ポイント +1
2. `肥料` → 成長ポイント +2
3. 成長ポイントが閾値を超えるたびに `growthStage` が1つ進む
4. `growthStage` が `maxGrowthStage` に達すると `harvestable` 状態になる

---

## 6. UI/UXデザイン方針

### 6.1 ビジュアルスタイル

- **テイスト**: 温かみのある、カジュアルなイラスト風
- **配色**: 緑・茶・青を基調とした自然をイメージしたカラーパレット
- **作物の表現**: 画像生成AIで作成したイラストを使用。各作物の成長段階ごとに画像を用意する

### 6.2 作物画像アセット仕様

画像生成AIを使用して、各作物の成長段階ごとのイラストを作成する。

| 項目 | 仕様 |
|------|------|
| フォーマット | PNG（透過背景推奨） |
| サイズ | 256×256px（表示時はCSSでスケール） |
| テイスト | 統一されたイラスト調（リアル寄りよりもカジュアルなイラスト風） |
| 命名規則 | `{作物名}-{段階番号}.png`（例: `tomato-1.png`） |

**必要な画像一覧:**

| 作物 | 段階1（種/芽） | 段階2（苗） | 段階3（成長） | 段階4（実り） |
|------|--------------|------------|-------------|-------------|
| トマト | 種まき直後の芽 | 小さな苗 | 花が咲いた状態 | 赤いトマトが実った状態 |
| いちご | 種まき直後の芽 | 葉が広がった苗 | 白い花が咲いた状態 | 赤いいちごが実った状態 |

### 6.3 カラーパレット

| 用途 | カラーコード | 説明 |
|------|------------|------|
| 背景 | `#F5F0E8` | 温かみのあるベージュ |
| 畑の土 | `#8B6914` | 耕された土の茶色 |
| 未使用の土 | `#A0926B` | グレーがかった茶色 |
| アクセント | `#4CAF50` | 植物の緑 |
| ボタン（プライマリ） | `#2E7D32` | 濃い緑 |
| ボタン（課金） | `#FF9800` | オレンジ |
| テキスト | `#333333` | ダークグレー |
| コイン | `#FFD700` | ゴールド |

### 6.4 レスポンシブ対応

| 画面サイズ | レイアウト |
|-----------|-----------|
| モバイル（〜480px） | 畑グリッド: 2列、アクションボタン: 縦並び |
| タブレット（481px〜768px） | 畑グリッド: 3列、アクションボタン: 横並び |
| デスクトップ（769px〜） | 畑グリッド: 4列、アクションボタン: 横並び |

### 6.5 フィードバック

- アクション実行時にトースト通知で結果を表示（例: 「水をやりました！🌱」）
- 無効なアクション時はボタンを非活性にし、理由をツールチップで表示
- 成長段階が進む際に軽いアニメーション効果

---

## 7. 初期データ

### 7.1 ゲーム開始時の初期状態

```json
{
  "fertilizer": 3,
  "farmSize": 4,
  "cells": [
    { "id": 0, "status": "empty", "crop": null, "growthPoints": 0 },
    { "id": 1, "status": "empty", "crop": null, "growthPoints": 0 },
    { "id": 2, "status": "empty", "crop": null, "growthPoints": 0 },
    { "id": 3, "status": "empty", "crop": null, "growthPoints": 0 }
  ],
  "harvestLog": []
}
```

---

## 8. デプロイ要件

### 8.1 GitHub Pages

- `main` ブランチから GitHub Actions を使って自動ビルド&デプロイ
- `vite.config.ts` に `base` パスを設定（リポジトリ名に対応）
- ビルドコマンド: `npm run build`
- 出力ディレクトリ: `dist/`

### 8.2 デプロイ手順

1. `main` ブランチにコードをプッシュ
2. GitHub Actions が自動で `npm run build` を実行し `dist/` を生成
3. GitHub リポジトリの Settings > Pages で Source を「GitHub Actions」に設定

---

## 9. ディレクトリ構成（想定）

```
virtual-saibai/
├── docs/
│   └── SPECIFICATION.md          # 本ドキュメント
├── public/
│   └── assets/
│       └── crops/                # AI生成画像（作物の成長段階ごと）
│           ├── tomato-1.png
│           ├── tomato-2.png
│           ├── tomato-3.png
│           ├── tomato-4.png
│           ├── strawberry-1.png
│           ├── strawberry-2.png
│           ├── strawberry-3.png
│           └── strawberry-4.png
├── src/
│   ├── components/
│   │   ├── Header.tsx             # ヘッダー（ステータス表示）
│   │   ├── FarmGrid.tsx           # 畑グリッド
│   │   ├── FarmCell.tsx           # 畑の個別マス
│   │   ├── CropDisplay.tsx       # 作物の表示（AI生成画像）
│   │   ├── ActionButtons.tsx      # アクションボタン群
│   │   └── Notification.tsx       # 通知表示
│   ├── pages/
│   │   ├── FarmPage.tsx           # 栽培画面ページ
│   │   └── ShopPage.tsx           # ショップページ
│   ├── data/
│   │   └── crops.ts               # 作物定義データ
│   ├── hooks/
│   │   └── useGameState.ts        # ゲーム状態管理カスタムフック
│   ├── types/
│   │   └── index.ts               # 型定義
│   ├── utils/
│   │   └── storage.ts             # localStorage ユーティリティ
│   ├── App.tsx                    # ルーティング設定
│   ├── index.css                  # Tailwind CSSエントリー
│   └── main.tsx                   # エントリーポイント
├── index.html
├── package.json
├── tailwind.config.js
├── postcss.config.js
├── tsconfig.json
├── vite.config.ts
└── README.md
```

---

## 10. 開発フェーズ

### Phase 1: 基盤構築

- [ ] プロジェクト初期化（Vite + React + TypeScript + Tailwind CSS）
- [ ] React Router（HashRouter）の設定
- [ ] 型定義の作成
- [ ] 作物データの定義
- [ ] ゲーム状態管理フック（`useGameState`）の実装

### Phase 2: アセット準備

- [ ] 作物画像の生成（画像生成AIを使用）
  - トマト: 成長段階1〜4の画像
  - いちご: 成長段階1〜4の画像
- [ ] 畑・土のビジュアル素材

### Phase 3: UI構築

- [ ] `Header` コンポーネント
- [ ] `FarmGrid` / `FarmCell` コンポーネント
- [ ] `CropDisplay` コンポーネント（AI生成画像表示）
- [ ] `ActionButtons` コンポーネント
- [ ] レスポンシブレイアウト（モバイルファースト）

### Phase 4: アクション実装

- [ ] 耕す機能
- [ ] 種を植える機能（作物選択UI含む）
- [ ] 水やり機能
- [ ] 肥料機能
- [ ] 収穫機能（収穫演出 + 履歴記録）
- [ ] アクションのバリデーション（前提条件チェック）

### Phase 5: ショップ・仕上げ

- [ ] `ShopPage` ページコンポーネント
- [ ] アイテム購入機能（モック）
- [ ] 通知・フィードバック表示
- [ ] 成長段階変化のアニメーション効果
- [ ] `localStorage` への保存/読み込み

### Phase 6: デプロイ

- [ ] GitHub Actions ワークフロー設定（main → GitHub Pages）
- [ ] `vite.config.ts` の `base` 設定
- [ ] 動作確認

---

## 11. 非機能要件

| 項目 | 要件 |
|------|------|
| パフォーマンス | 初回ロード3秒以内 |
| アクセシビリティ | ボタンにaria-labelを付与、キーボード操作対応 |
| ブラウザ対応 | Chrome / Firefox / Safari / Edge の最新版 |
| データ永続化 | localStorage によるセーブ・ロード |
| セキュリティ | フロントエンドのみのため、特別な対策は不要（課金はモック） |

---

## 12. 将来的な拡張候補（スコープ外）

以下は本プロトタイプのスコープ外だが、将来的に検討する機能：

- **スマホアプリ化**（React Native / Capacitor による iOS / Android 展開）
- 収穫した作物の配送システム（EC/物流との連携）
- AI による栽培アドバイス機能
- 天候システム（ランダムイベント）
- 季節の概念と作物の旬
- ユーザー認証・クラウド保存
- マルチプレイヤー（友達の畑を訪問）
- 実際の課金システム（Stripe / App Store / Google Play）
- 図鑑・コレクション機能
- 実績・バッジシステム
- 作物の種類の拡充
